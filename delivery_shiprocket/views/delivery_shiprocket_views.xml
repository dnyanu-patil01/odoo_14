<?xml version="1.0" encoding="UTF-8"?>
<odoo>
   <record id="view_delivery_carrier_form_with_provider_shiprocket" model="ir.ui.view">
      <field name="name">delivery.carrier.form.provider.shiprocket</field>
      <field name="model">delivery.carrier</field>
      <field name="inherit_id" ref="delivery.view_delivery_carrier_form" />
      <field name="arch" type="xml">
         <xpath expr="//page[@name='destination']" position="before">
            <page string="Shiprocket Configuration" attrs="{'invisible': [('delivery_type', '!=', 'shiprocket')]}">
               <group>
                  <group>
                     <field name="shiprocket_channel_id" attrs="{'required': [('delivery_type', '=', 'shiprocket')]}" />
                     <field name="shiprocket_payment_mode" attrs="{'required': [('delivery_type', '=', 'shiprocket')]}" />
                  </group>
               </group>
            </page>
         </xpath>
         <!-- Added Chatter Log -->
         <xpath expr="//sheet" position="after">
				<div class="oe_chatter">
					<field name="message_follower_ids" widget="mail_followers"/>
					<field name="message_ids" widget="mail_thread"/>
				</div>
			</xpath>
      </field>
   </record>
</odoo>